
//if this or that is true then
//if (this || that) {
    //prompt("Anything Else")



















/*
let list = document.getElementById("unlist");
let input = '';
for (i = 0; i < input.length; i++) {
    var li = document.createElement('li');
    li.innerText = input[i];
    list.appendChild(li);
} */